import "package:flutter/material.dart";

class Results extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(
        title: Text('ሰግመ ማሰቢያ'),
      ),
      body: Text('እንኳን ደህና መጡ')
    );
  }
}